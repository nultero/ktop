package draw

import (
	"ktop/state"

	"github.com/gdamore/tcell/v2"
)

func tableOfProcs(scr tcell.Screen, stt *state.State, bounds [4]int) {
	// lw, rw, th, bh := bounds[0], bounds[1], bounds[2], bounds[3]

}
